# 医疗研究 Agent 实施日志

## 2026-07-26

### 启动与基线确认

- 按 UTF-8 完整读取两份开发文档。
- 确认冲突时以《医疗研究Agent开发实施方案》为准。
- 确认仓库无既有业务代码，不存在需要覆盖的用户实现。
- 确认远端仓库公开，实施中只使用匿名示例、Mock 数据和非敏感配置。
- 发现 Maven 3.9.11 默认运行时为 Java 8，而系统 `java` 为 Java 21。
- 发现 Docker 命令不可用，容器联调记为环境阻塞，不伪造验证结果。

### 实施计划

1. 建立版本化研究 Schema、研究规则、ModelRouter 和 Mock 模型。
2. 实现研究想法解析、澄清、三个观察性方向、方向确认和 PECO。
3. 使用固定 Mock PubMed 快照生成可追溯背景段落。
4. 填充受控 DOCX 模板并执行结构、文本和视觉检查。
5. 增加 Flyway、Compose、前端演示和关键测试。

### 实施结果

- 创建 Spring Boot 3.5.16 / Spring AI 1.1.8 / Java 21 单 Maven 工程。
- 本机补充便携 Temurin JDK 21.0.11（位于用户缓存，不进入仓库）。
- 创建 Vue 3、TypeScript、Vite、Element Plus 前端纵向演示。
- 创建研究 Schema、ModelRouter、MockResearchModel、MockPubMedGateway、输出校验和敏感内容规则。
- 创建受控 DOCX 模板生成器、填充服务和匿名导出样例。
- 创建 PostgreSQL/pgvector Flyway V1 迁移和 PostgreSQL/MinIO Compose。

### 验证结果

- `mvn test`：5 通过，0 失败，1 跳过（Docker 不可用）。
- Spring Boot JAR 实际启动，三个 HTTP 端点调用通过。
- `npm run lint`：通过。
- `npm run typecheck`：通过。
- `npm run test`：1 通过。
- `npm run build`：通过，有单包超过 500 kB 警告。
- DOCX 结构检查：通过。
- DOCX LibreOffice 渲染：失败/超时，未完成视觉验收。
- 最终引用一致性核对发现并修正 CIT-001 的错误 PMID/DOI；两条快照现已按 PubMed 题名、期刊、PMID 和 DOI 复核。

### DOCX 视觉验收补充

- 用户安装 LibreOffice 26.2.4.2 后重新执行渲染。
- 发现文档技能渲染脚本生成的 Windows `UserInstallation` URI 不兼容，改用 LibreOffice 绝对路径和标准 `file:///` URI 隔离转换。
- 使用最终 DOCX 生成 1 页 PDF 和 PNG，并以原始分辨率逐页检查。
- 最终页面无文字裁切、重叠、表格破损、缺字或异常分页；视觉验收通过。

### 阶段判定

阶段 0 尚未达到退出条件。技术原型已完成，但真实模型、真实 PubMed、真实脱敏模板和专家确认均缺少外部输入。

## 2026-07-26 阶段 1 启动

### 已实施

- 增加 Spring Security、BCrypt、医院代码登录和 HttpOnly/SameSite 会话 Cookie。
- 增加 CSRF 防护、首次改密、登录失败锁定、会话撤销和账号禁用。
- 增加平台医院管理、医院用户管理和角色边界。
- 增加医院隔离课题 CRUD、幂等键、乐观锁和操作审计。
- 增加 Flyway V2 身份、会话、审计和幂等表。
- 增加前端登录、首次改密和本院课题管理面板。

### 测试

- 后端：12 个测试通过，1 个 Testcontainers/Flyway 测试因 Docker 不可用跳过。
- 新增覆盖：Cookie/CSRF HTTP 闭环、五次失败锁定、会话哈希、改密撤销会话、首次改密限制、跨医院读取/修改阻断、幂等和乐观锁。
- 前端：ESLint、TypeScript、Vitest 和 Vite 构建通过。
- 前端生产包仍有超过 500 kB 的拆包警告。

### 阶段判定

阶段 1 已开始但未达到退出条件。当前默认运行使用内存适配器；PostgreSQL 业务仓储、课题成员、文件上传/MinIO 和数据库级跨医院测试仍待完成。

### 本地中间件联调

- 复用 `D:\develop\postgresql18`，PostgreSQL 18.4 只监听本机回环地址。
- 创建最小权限 `medical_agent` 本地角色和同名数据库。
- 首次 Flyway 因应用角色无权创建扩展而失败并回滚；由管理员预装 pgvector 0.8.0 后重试成功，未向应用角色授予超级用户。
- Flyway V1/V2 实际迁移成功，共创建 12 张 public 表。
- Flyway 11.7.2 对 PostgreSQL 18.4 提示最高已测试到 PostgreSQL 17，记录为兼容性观察项。
- 启动 `D:\develop\minio\minio.exe`，API `127.0.0.1:9000`、控制台 `127.0.0.1:9001`，健康检查返回 HTTP 200。
- MinIO 根凭据使用 Windows DPAPI 加密保存在中间件目录，不进入 Git 和普通日志。

### 阶段 1 PostgreSQL 与 MinIO 第二切片

- 将课题持久化抽象为 `ProjectRepository`，提供内存和 PostgreSQL 两套实现。
- PostgreSQL 课题 SQL 全部显式包含 `hospital_id`，创建支持幂等记录，更新使用版本号条件。
- 新增本地 PostgreSQL 显式联调测试，真实验证跨医院读取/更新阻断、幂等回放和乐观锁。
- 新增 Flyway V3 `project_file` 元数据表，本地数据库已迁移至 V3，共 13 张 public 表。
- 接入 MinIO Java SDK 9.0.1，应用启动时幂等创建 `medical-agent-files` 存储桶。
- 新增上传基础流水线：20MB 限制、允许类型、MIME、魔数、UTF-8、EICAR 测试串、文件名清洗、SHA-256 和敏感内容检测。
- 对象键固定为医院/课题隔离的 quarantine 路径；数据库写入失败时执行对象补偿删除。
- 本地 MinIO 真实完成对象写入、读取和删除，测试对象已清理。
- 创建一次性临时 PostgreSQL 数据库，从空库完整执行 V1/V2/V3，确认生成 13 张 public 表后删除临时库。
- 全量后端测试为 19 个：16 个常规通过、3 个环境测试默认跳过；PostgreSQL 和 MinIO 环境测试随后显式启用，2 个均通过。
- 前端 ESLint、TypeScript、Vitest 和生产构建复验通过；仍保留既有大包体警告。
- 构建需显式使用 `D:\develop\jdk21`；系统 Maven 默认 `JAVA_HOME` 仍指向 JDK 8。

### 阶段 1 身份持久化与课题成员

- 新增身份仓储边界及内存/PostgreSQL 实现，覆盖医院、用户、密码状态、锁定状态和会话。
- 登录、令牌认证、改密、会话撤销、医院和用户管理不再直接依赖进程内存。
- 新增操作审计仓储边界，PostgreSQL profile 将审计事件写入 `operation_audit`。
- 真实写入测试发现 PostgreSQL JDBC 无法自动推断 `Instant` 参数；统一改为显式 `Timestamp`，同步修正身份、会话、审计和文件元数据。
- 新增 Flyway V4 `project_member` 表及 `OWNER`、`EDITOR`、`VIEWER` 权限。
- 课题创建者自动成为所有者；列表、详情、更新和文件上传均复用成员可见性边界。
- 新增成员添加/列表 API；仅课题所有者或医院管理员可以管理成员。
- 真实 PostgreSQL 测试验证身份、会话、审计、文件元数据和成员关系持久化，测试数据已清理。
- 真实 Spring Security + PostgreSQL API 测试验证非成员不可见、加入查看者后可见。
- 一次性临时数据库从空库完整执行 V1/V2/V3/V4，确认生成 14 张 public 表后删除。
- 全量后端测试为 22 个：17 个常规通过、5 个环境测试默认跳过；随后显式启用 4 个 PostgreSQL/MinIO 测试，全部通过。

### 阶段 1 可观测性、OpenAPI 与管理前端

- 新增 Trace ID 请求过滤器，只接受 8～64 位安全字符；否则生成随机标识。
- `X-Trace-Id` 同时进入响应头、统一响应体、错误体和 MDC 日志格式。
- 新增医院范围审计查询；`HOSPITAL_ADMIN` 只能读取本院，`AUDIT_ADMIN` 按所属范围读取。
- 接入 springdoc-openapi 2.8.17，显式启用 `/v3/api-docs` 和 `/swagger-ui.html`。
- 前端增加成员查看/添加、PDF/DOCX/TXT/MD 上传、安全状态展示和审计记录。
- 新增 GitHub Actions CI，使用 Java 21、Node 24，并执行后端、Flyway 和前端检查。
- 全量后端测试为 23 个：18 个常规通过、5 个环境测试默认跳过；4 个真实环境测试显式启用后全部通过。
- 真实受认证 API 已完成“创建课题→添加成员→上传 MinIO→写文件元数据→查询审计”闭环，对象和测试数据均已清理。

## 2026-07-27 阶段 1 收口与阶段 2 启动

### 阶段 1 收口

- 前端增加正式 Vue Router、Pinia 会话恢复、可复用角色权限指令和路由懒加载。
- 文件扫描从校验器拆为 `MalwareScanner` 边界；实现基础签名扫描和 ClamAV INSTREAM 适配器。
- 引入 Apache PDFBox 3.0.8，配合 Apache POI 提取 PDF/DOCX 文本，并限制 PDF 200 页、
  提取文本 200,000 字符。
- Flyway V5 增加扫描引擎、提取字符数和提取状态；工作台显示对应结果。
- 真实 PostgreSQL/MinIO API 改用 DOCX 上传，验证扫描、文本提取、敏感检测、对象写入和元数据。
- 空库验证发现应用角色无权创建数据库和 pgvector 扩展；按最小权限调整为管理员创建随机临时库
  并预装扩展、应用角色执行全部 Flyway，验证后强制删除临时库。
- 阶段 1 退出条件全部完成，转入阶段 2。

### 阶段 2 第一切片

- Flyway V6 增加 Agent 幂等、取消、租约、超时、错误和完成时间字段，以及可补发事件表。
- 实现内存/PostgreSQL Agent 仓储、后台定时 Worker、版本抢占和租约过期恢复。
- 持久化实现 STEP_01～STEP_06；方向确认是明确人工确认点。
- 实现任务创建、列表、详情、确认方向、取消、重试和 SSE API，SSE 事件 ID 支持断线补发。
- 前端工作台增加 Agent 启动、状态、方向确认、取消、重试和刷新恢复。
- 权限复核发现 VIEWER 可经文件/Agent 服务调用只读 `get` 后执行写入，新增 `requireEditable`
  统一修复并增加回归断言。
- 默认全量测试 31 个：25 个通过、6 个环境跳过；真实环境全量 30 个通过，只有
  Docker/Testcontainers 测试跳过。
- 本地数据库已迁移到 V6；真实 Agent API 从创建到 PECO 完成，步骤和至少 9 个事件落库。
- 所有真实测试数据、MinIO 对象和随机临时数据库均已清理，应用进程未常驻。

### 阶段 2 第二切片：澄清门禁、Prompt 与研究类型规则

- 新增 `PromptTemplateRegistry` 和三个 UTF-8 Prompt 资源，版本分别为
  `research-idea-analysis/v1`、`research-directions/v1`、`research-question/v1`。
- Mock 模型同样接收版本化 Prompt 元数据；没有真实模型配置时仍保持确定性执行。
- 将原来一次 Worker 内连续完成 STEP_01～04 的逻辑拆开：STEP_02 后进入
  `STEP_03_ASK_CLARIFICATION`，必须由医生逐项回答后才排队执行 STEP_04。
- 新增澄清提交 API、步骤人工确认记录和 `WAITING_CLARIFICATION` /
  `CLARIFICATIONS_CONFIRMED` 事件。
- 前端工作台增加四项澄清表单，方向卡片只在 STEP_05 显示。
- 新增 `ObservationalStudyRuleService`，按横断面、队列和病例对照校验必填字段，
  形成版本化 `designAssessment`，不输出研究质量分数。
- 真实 PostgreSQL API 已验证“建任务→回答澄清→生成方向→确认→PECO/规则评估”闭环。
- 默认全量 33 个测试中 27 个通过、6 个环境跳过；真实环境 32 个通过，仅
  Docker/Testcontainers 跳过。前端 lint、类型检查、Vitest 和生产构建均通过。
- 回归完成后任务、步骤、事件、文件、MinIO 对象和随机临时数据库计数均为 0。

### 阶段 2 模型接入准备：DeepSeek

- 新增 `DeepSeekResearchModel` 和条件路由，调用 OpenAI 兼容
  `POST /chat/completions`，使用 JSON Output 和 `research-analysis/v1` 结构。
- 按 2026-07-27 官方 API 使用 `deepseek-v4-flash`；旧 `deepseek-chat` 不再作为默认值。
- 增加显式外部调用安全门：只有 `MEDICAL_MODEL_MODE=deepseek` 且
  `MEDICAL_MODEL_EXTERNAL_ENABLED=true` 时才创建真实模型适配器。
- 凭证支持环境变量或本地只读文件，异常和测试输出不包含令牌；`deepseek_token.txt`
  已加入 `.gitignore`。
- WireMock 契约测试覆盖 Bearer 鉴权、模型名、JSON 模式和结构化响应解析；安全门关闭时
  验证拒绝真实调用。
- 使用虚构研究想法完成一次真实 `deepseek-v4-flash` 调用，输出通过三个观察性研究方向和
  `ResearchOutputValidator` 校验。
- 新增后端默认全量为 36 个测试：29 个通过、7 个外部环境测试跳过、0 失败；DeepSeek
  真实测试按环境变量显式启用，单独执行通过。

### 阶段 2 第三切片：多轮澄清、权限/SSE 与浏览器测试

- 新增 Flyway V7 `ai_agent_clarification_round`，每轮答案按任务递增编号并记录问题、
  有效答案、来源步骤、提交人和时间，轮次提交与任务状态切换使用同一数据库事务。
- 澄清提交同时支持 STEP_03 和 STEP_05；在方向确认页修改答案后，旧 STEP_05 运行记录
  标记为 `SUPERSEDED`，任务返回 STEP_04 重新生成方向。
- 人工等待后重新排队时重置执行超时，避免医生等待超过原始任务超时后立即失败。
- 新增澄清历史查询 API；前端展示折叠式轮次历史，并允许从 STEP_05 保存下一轮答案。
- SSE 超时改为可配置项；`Last-Event-ID` 只接受非负整数，不合法请求返回 400。
- 新增真实 HTTP 权限测试，覆盖非成员、VIEWER、OWNER 对任务、澄清历史、SSE、提交和
  方向确认的边界。
- 新增 Playwright Chromium 流程及 GitHub Actions 步骤，匿名模拟 API 验证两轮澄清、
  答案修改、历史显示和方向重生成。
- 默认后端全量 38 个测试：31 个通过、7 个环境跳过；本机 PostgreSQL/MinIO 全量
  36 个通过，仅 Docker 和 DeepSeek 计费测试跳过。
- PostgreSQL 18 本地库迁移到 V7，共 16 张 public 表；空库 V1→V7 和真实两轮 Agent API
  均通过，任务、轮次、步骤、事件和临时数据库清理后均为 0。
- 前端 lint、类型检查、Vitest、Playwright 和生产构建全部通过；生产依赖审计为 0 漏洞，
  保留现有主包超过 500 kB 的拆包警告。

### 阶段 2 第四切片：规则配置化与合成匿名评测

- 将观察性研究必填字段、答案问题标记、未解析值标记和研究类型解释迁移到
  `observational-study-rules-v1.json`。
- 新增 `ObservationalStudyRuleRegistry`，只加载显式注册版本；字段定义必须与白名单完全
  一致，三类研究设计必须全部存在，重复/未知字段和空配置会拒绝加载。
- 增加兼容测试：V1 可增加不影响语义的元数据；版本漂移、缺失研究类型、未知字段和重复
  必填字段均失败关闭。
- 新增 5 个 `SYNTHETIC_ANONYMOUS` 合成开发案例和加载校验，不包含患者姓名、身份证、
  住院号或手机号字段；明确不把它们描述为医院历史课题。
- 新增 `ResearchModelEvaluationService`，只评估 Schema、关键词、观察性研究类型、
  澄清问题数量和缺失信息数量，不生成医学质量分数。
- 确定性 Mock 对 5 个案例结构评测 5/5 通过。
- 显式使用 DeepSeek 对前 2 个案例执行真实比较，共调用 2 次；Mock 与 DeepSeek 均为
  2/2 通过，SYN-002 缺失信息数存在 4 对 2 的差异，等待专家判断。
- 比较结果写入 `artifacts/evaluation/anonymous-model-comparison-v1.md`，不保存模型原文。
- 默认后端全量 43 个测试：35 个通过、8 个环境跳过；本机 PostgreSQL/MinIO 全量
  40 个通过，仅 Docker 和两项 DeepSeek 外部调用测试跳过，0 失败。

### 阶段 2 第五切片：STEP07 版本化检索策略与人工确认

- 新增确定性 `SearchStrategyService`，从已确认 PECO 生成 `search-strategy/v1`，
  保存结构化检索概念、`pubmed-query/v1`、目标数据库、筛选条件、覆盖限制和生成器版本。
- STEP06 不再直接完成任务；Worker 保存 STEP07 待确认运行记录，任务进入
  `WAITING_CONFIRMATION`，并发布 `WAITING_SEARCH_STRATEGY`。
- 新增 `POST /api/agent/tasks/{taskId}/confirm-search-strategy`。医生可在 4000 字以内
  修订 PubMed 检索式；系统保留生成式和确认式，拒绝空值、超长值和非法控制字符。
- 内存与 PostgreSQL 仓储均以原子状态转换完成任务，并把最终检索式、确认人、确认时间写回
  STEP07；并发重复确认返回冲突。
- HTTP 权限回归确认 VIEWER 可查看但不能确认，OWNER 可确认；新增两类 SSE 事件和审计动作。
- 前端新增结构化概念表、数据库/版本/限制展示、可编辑检索式和完成后的已确认版本展示。
- 新增检索策略单元测试和 Playwright Chromium 流程；前端 lint、类型检查、Vitest、2 个
  浏览器流程和生产构建全部通过，保留既有主包超过 500 kB 告警。
- 后端默认全量 46 个测试：38 个通过、8 个环境跳过；本机 PostgreSQL/MinIO 全量
  43 个通过，仅 Docker 和两项 DeepSeek 外部调用测试跳过，0 失败。
- 真实空库 V1→V7、PostgreSQL STEP07 状态/检索式持久化和 MinIO 上传闭环通过；任务、
  轮次、步骤、事件和随机临时数据库均已清理。

### 阶段 2 第六切片：STEP08 PubMed 检索、验证与可重放存档

- STEP07 确认后以原子状态转换排队进入 `STEP_08_SEARCH_PUBMED`；Worker 执行成功后才完成
  任务，并发布 `LITERATURE_SEARCH_COMPLETED` 和 `TASK_COMPLETED`。
- 新增 Mock/Live 双模式 `PubMedSearchGateway`。Live 模式使用 POST 调用 NCBI ESearch、
  ESummary、EFetch，必须配置已注册的工具名和联系邮箱，支持可选 API Key、请求节流、
  `Retry-After` 及仅针对瞬时错误的有界退避重试。
- 对三段响应执行 PMID 集合、重复项及题名映射校验；PubMed XML 的合法 DOCTYPE 可解析，
  外部 DTD、外部实体和 XInclude 均被禁用。任何协议、校验或存储失败都显式失败，不生成
  未验证结果。
- Flyway V8 新增检索任务、文献主记录和课题文献关联三张表。原始响应以
  `ncbi-eutils-raw/v1` 写入 MinIO，并保存对象键、内容类型、工具版本和 SHA-256；
  PostgreSQL 写入在事务中完成，失败时补偿删除对象。
- 前端在策略确认后继续监听 SSE，展示检索统计、工具版本、原始响应哈希、PMID、题名、
  期刊、日期、证据范围及限制；Playwright 覆盖 STEP07→STEP08 完整界面状态。
- 新增 WireMock 协议/重试/失败关闭测试、Service 回放测试和条件式真实 PubMed 测试。
  因未提供 NCBI 注册联系邮箱，真实联机测试保持跳过，没有伪造邮箱。
- 后端默认全量 52 个测试：43 个通过、9 个环境测试跳过；本机 PostgreSQL/MinIO 全量
  48 个通过，仅 Docker、两项 DeepSeek 外部调用和一项 PubMed 联机测试跳过，0 失败。
- 空库 V1→V8 迁移为 19 张 public 表；真实 PostgreSQL/MinIO 检索、哈希和关联闭环通过。
  任务、轮次、步骤、事件、检索、文献、关联及随机临时数据库均已清理。
- 前端 ESLint、TypeScript、Vitest 1 项、Playwright Chromium 2 项、生产构建和生产依赖
  审计全部通过；生产依赖 0 漏洞，保留既有主包超过 500 kB 告警。

### 阶段 2 第七切片：STEP09 ClinicalTrials.gov API v2

- 将 STEP08 完成状态改为原子排队 `STEP_09_SEARCH_CLINICAL_TRIALS`；只有 STEP09 成功后
  才完成任务并发布 `CLINICAL_TRIALS_SEARCH_COMPLETED` 和 `TASK_COMPLETED`。
- 从已确认的 PECO 人群、暴露和结局概念确定性生成 `clinicaltrials-query/v1`，移除
  PubMed 字段语法，不调用模型翻译或自由生成检索词。
- 新增 Mock/Live 双模式 ClinicalTrials.gov API v2 客户端，调用 `/api/v2/studies`，
  支持参数编码、保守节流、`Retry-After`、有界退避和 12 小时 Caffeine 缓存。
- 客户端验证 `NCT` 加 8 位数字、题名、注册状态、研究类型及重复 ID；提取阶段、疾病、
  干预、主要终点、申办者、日期、入组数、国家、结果可用性和关联 PMID。
- Flyway V9 新增 `clinical_trial_record` 与 `project_clinical_trial`。医院内按 NCT ID
  去重，原始 API JSON 写入 MinIO，并在检索任务中保存查询、数量、工具/数据版本和 SHA-256。
- 注册记录使用 `TRIAL_REGISTRY` 来源类型及 `REGISTRY_METADATA_ONLY` /
  `REGISTRY_RESULTS_AVAILABLE` 范围，明确不把注册信息冒充同行评议论文证据。
- 前端增加 ClinicalTrials.gov 检索式、注册状态、研究类型、入组数、缓存/数据版本、
  NCT 官方链接、限制说明和 NLM 数据来源声明；Playwright 覆盖 STEP07→STEP09。
- WireMock 覆盖参数、映射、缓存、瞬时重试和重复 NCT 失败关闭；Service 覆盖原始响应
  回放、哈希和显式失败；已知 NCT 的官方 API v2 真实联机测试通过。
- 后端默认全量 58 个测试：48 个通过、10 个环境测试跳过；PostgreSQL、MinIO 和
  ClinicalTrials.gov 实机全量 54 个通过，仅 Docker、两项 DeepSeek 和一项 PubMed
  联机测试跳过，0 失败。
- 空库 V1→V9 为 21 张 public 表；真实 PostgreSQL/MinIO 两数据源检索闭环通过。
  Agent、检索、文献、注册研究、关联、对象及随机临时数据库测试数据均已清理。
- 前端 ESLint、TypeScript、Vitest 1 项、Playwright Chromium 2 项、生产构建和生产依赖
  审计全部通过；生产依赖 0 漏洞，保留既有主包超过 500 kB 告警。

### 阶段 2 第八切片：STEP10 Crossref 文献验证与跨来源关联

- 将 STEP09 完成状态改为原子排队 `STEP_10_VALIDATE_LITERATURE`；只有 STEP10 成功后
  才完成任务并发布 `LITERATURE_VALIDATION_COMPLETED` 和 `TASK_COMPLETED`。
- 新增 Mock/Live 双模式 Crossref REST 客户端，使用 `/works/{doi}` 查询单篇元数据；
  DOI 规范化后进入 24 小时缓存，public pool 采用 250 ms 最小间隔，配置真实
  `CROSSREF_MAILTO` 后采用 110 ms 最小间隔。
- 客户端使用标准 `application/json`，携带版本化 User-Agent；支持 `Retry-After` 和
  有界指数退避。404 作为可审计的“未找到”结果，错误响应、DOI 不一致、题名缺失或非法
  JSON 均失败关闭。
- STEP10 比较 PubMed/Crossref 的 DOI、题名、作者、期刊和发表年份。DOI 或题名不一致
  标记 `MISMATCH`；辅助元数据不同标记 `VERIFIED_WITH_METADATA_DIFFERENCES`；
  无 DOI 与 Crossref 未找到分别记录，不伪造验证成功。
- 从 ClinicalTrials.gov 公开 `linkedPmids` 建立
  `REGISTRY_REFERENCES_PUBLICATION` 关联；当前 PubMed 检索集内命中标记 `RESOLVED`，
  未命中标记 `UNRESOLVED_PUBMED`，并继续声明注册表信息不等同于同行评议证据。
- Flyway V10 新增验证任务、引文验证和证据来源关联三张表。Crossref 原始响应批次以
  JSON 写入 MinIO，数据库保存对象键、SHA-256、工具版本、请求数、缓存命中数和字段级判定；
  失败时补偿删除对象。
- WireMock 覆盖 DOI 路由、可选 `mailto`、User-Agent、映射、缓存、503 重试、404 和
  返回 DOI 不一致；Service 覆盖验证分类、原始响应哈希、跨来源关联和显式故障。
- Crossref public pool 对已知 DOI `10.1056/NEJMoa2204233` 的真实联机测试通过；未使用
  虚构联系邮箱。真实 PostgreSQL/MinIO API 闭环验证 2 篇引文、2 条注册研究关联和对象清理。
- 后端默认全量 64 个测试：53 个通过、11 个环境测试跳过；真实环境矩阵 60 个通过，
  仅 Docker/Testcontainers、两项 DeepSeek 外部调用和一项 PubMed 联机测试未执行，0 失败。
- 空库 V1→V10 为 24 张 public 表；主库迁移到 V10。任务、步骤、事件、检索、文献、
  注册研究、验证批次、引文判定、证据关联、MinIO 对象和随机临时数据库测试数据均已清理。
- 前端新增 STEP10 验证汇总、引文状态、DOI 链接和注册研究—论文关联表；ESLint、
  TypeScript、Vitest 1 项、Playwright Chromium 2 项、生产构建和生产依赖审计均通过，
  生产依赖 0 漏洞，保留既有主包超过 500 kB 告警。

### 阶段 2 第九切片：STEP11 相似研究与潜在研究空白

- 将 STEP10 完成状态改为原子排队 `STEP_11_ANALYZE_SIMILAR_RESEARCH`；只有 STEP11
  成功后才完成任务并发布 `SIMILAR_RESEARCH_ANALYSIS_COMPLETED` 和 `TASK_COMPLETED`。
- 新增版本化确定性算法 `deterministic-peco-overlap/v1`，对研究对象、暴露/干预、
  对照、结局和研究设计五个维度加权匹配，输出 0～100 分及 HIGH/MODERATE/LOW 分层。
- 输入严格限定为已确认 PECO/检索策略、STEP08/09 来源和 STEP10 核验结果。PubMed 只纳入
  `VERIFIED` / `VERIFIED_WITH_METADATA_DIFFERENCES`；其他状态计入排除数，不参与相似性判定。
- PubMed 文章和 ClinicalTrials.gov 注册研究使用不同来源类型、核验状态和证据范围；
  注册 PMID/NCT 链接随比较记录保存，不把注册元数据冒充发表证据。
- 对五个维度中当前来源均未匹配的部分生成版本化潜在空白编码、建议、形成依据和来源标识。
  结论在有/无高度相似来源时都明确不构成创新性/优先权证明，也不代表完成全部数据库和灰色文献检索。
- Flyway V11 新增分析任务、来源比较和研究空白建议三张表，保存输入 SHA-256、数据库范围、
  算法版本、来源分层、字段差异、结论和完整结果 JSON。
- 单元测试覆盖核验引文筛选、注册来源隔离、跨来源链接、相似度分层、潜在空白、输入哈希和
  禁止创新性声明；内存/HTTP 工作流覆盖 STEP10→STEP11 状态转换和完成事件。
- PostgreSQL/MinIO 真实 API 闭环验证 4 个来源比较及完整清理；空库 V1→V11 迁移为
  27 张 public 表，主库同步迁移到 V11。
- 前端新增 STEP11 来源比较、五维匹配、相似度、核验状态、结论和潜在空白面板；
  ESLint、TypeScript、Vitest 1 项、Playwright Chromium 2 项和生产构建均通过，
  保留既有主包超过 500 kB 告警。

### 阶段 2 第十切片：STEP12 观察性研究设计推荐与人工授权

- 将 STEP11 完成改为原子排队 `STEP_12_RECOMMEND_OBSERVATIONAL_DESIGN`；STEP12 生成推荐后
  进入 `WAITING_CONFIRMATION`，不再自动完成任务。
- 新增 `ObservationalDesignRecommendationService`，以已确认 PECO、澄清答案、版本化
  观察性研究规则和 STEP11 证据差异为输入，确定性比较 COHORT、CROSS_SECTIONAL、
  CASE_CONTROL 三种设计。
- `observational-design-rules/v1` 输出备选排序、0～100 规则评分、可行性、必填/缺失字段、
  推荐依据、各设计固有偏倚风险和证据注意事项；输入和结果使用 SHA-256 与 Schema 版本留痕。
- 研究类型、主要终点和进入正式方案生成授权是三个强制人工确认项。信息不完整或未明确授权时
  拒绝确认；VIEWER 只能查看，OWNER/EDITOR 可确认，重复/并发确认返回冲突。
- Flyway V12 新增推荐任务和设计备选两张表；生成结果不可变持久化，最终人工选择、确认人、
  确认时间与授权结果写入 Agent 输出及 STEP12 运行记录，避免双事实源。
- 新增 `POST /api/agent/tasks/{taskId}/confirm-observational-design`、等待/确认 SSE 事件和
  `AGENT_OBSERVATIONAL_DESIGN_CONFIRMED` 审计动作。
- 前端新增三设计比较、规则版本、输入哈希、偏倚风险、待解决项、主要终点修订和显式授权面板；
  未勾选授权或信息不完整时确认按钮不可用。
- 后端默认全量 66 个测试：55 个通过、11 个环境测试条件跳过、0 失败。真实 PostgreSQL/MinIO
  API 验证 STEP12 推荐任务、3 个备选设计、人工确认人/时间及权限边界；空库从 V1 连续迁移到
  V12 共 29 张 public 表，主库同步迁移到 V12。
- 前端 ESLint、TypeScript、Vitest 1 项、Playwright Chromium 2 项、生产构建和生产依赖审计
  全部通过；生产依赖 0 漏洞，保留既有主包超过 500 kB 告警。
- 测试结束后推荐任务、备选设计、Agent、课题和对象存储数据均已清理；主库相关业务表计数为 0，
  随机 Flyway 临时数据库已删除。

### 阶段 2 第十一切片：STEP13 分章节观察性研究方案草案

- STEP12 人工确认后不再直接完成任务，而是原子排队
  `STEP_13_GENERATE_PROTOCOL_SECTIONS`；重新设置任务超时并保留确认人、确认时间和最终选择。
- 新增 `ResearchProtocolGenerationService` 和
  `deterministic-observational-protocol/v1`，只接受已确认研究类型、主要终点和正式方案生成授权。
- 确定性生成课题名称、摘要、背景、研究现状、潜在空白、目标、假设、设计、研究对象、
  纳入/排除标准、变量和终点、数据收集、统计分析、偏倚控制、伦理与数据安全、进度、
  预期成果及参考文献共 18 个中文章节。
- 每个章节记录独立标识、排序、版本号、Markdown 内容、生成来源、证据状态、来源标识和
  待确认项。事实性证据章节继续标记摘要/元数据边界，不把注册信息冒充发表证据。
- 统计章节仅列出 STEP14 所需效应度量、分析集、混杂控制、缺失数据、敏感性分析和样本量
  参数清单，不自动猜测样本量、效应量、显著性或因果结论。
- 参考文献仅从 STEP11 已核验 PubMed 来源构建；PMID/DOI 不由模板或模型自由生成，并明确
  等待 STEP15 完成事实主张—引用依据验证。
- Flyway V13 新增方案、章节和章节版本三张表。初始生成事务内写入 1 个 DRAFT 方案、
  18 个章节和 18 个不可变 v1 版本，为后续手工编辑、Agent 重生成和专家确认留出版本链。
- 新增 `PROTOCOL_GENERATION_QUEUED` / `PROTOCOL_SECTIONS_GENERATED` 事件；前端新增章节折叠
  面板，展示方案标题、生成器版本、输入 SHA-256、来源、证据状态、生成依据及待确认项。
- 后端默认全量 67 个测试：56 个通过、11 个环境测试条件跳过、0 失败。真实 PostgreSQL/MinIO
  API 闭环验证 18 个章节与 18 个初始版本；空库从 V1 连续迁移到 V13 共 32 张 public 表，
  主库同步迁移到 V13。
- 前端 ESLint、TypeScript、Vitest 1 项、Playwright Chromium 2 项、生产构建和生产依赖审计
  全部通过；生产依赖 0 漏洞，保留既有主包超过 500 kB 告警。
- 测试结束后方案、章节、章节版本、Agent、课题及 MinIO 测试对象均已清理；主库相关业务表
  计数为 0，随机 Flyway 临时数据库已删除。

### 阶段 2 第十二切片：STEP14 统计分析计划草案

- STEP13 完成后不再直接结束任务，而是原子排队
  `STEP_14_GENERATE_STATISTICAL_DRAFT`；STEP14 成功后才发布
  `STATISTICAL_DRAFT_GENERATED` 和最终 `TASK_COMPLETED`。
- 新增 `StatisticalAnalysisDraftService` 与
  `deterministic-observational-statistics/v1`，只接受 STEP13 完整方案、已确认研究类型和
  已确认主要终点，重复执行使用输入 SHA-256 幂等回放。
- 按 COHORT、CROSS_SECTIONAL、CASE_CONTROL 分别生成条件化主要分析候选、效应量和
  设计特异诊断/敏感性计划；同时覆盖描述性统计、次要终点、协变量、潜在混杂、分层、亚组、
  缺失数据、多重比较、置信区间和统计软件候选。
- 样本量只生成 8 项设计适配参数需求；`value_status` 全部为
  `MISSING_NEEDS_INPUT`，`value` 全部为空，不填入假设 alpha、power、效应量或最终样本量。
- STEP13 的统计分析章节 v1 不被覆盖。STEP14 新增不可变 v2 版本，并以
  `current_version_no=1` 条件更新当前版本；若已存在人工/其他版本则事务失败，避免静默覆盖。
- Flyway V14 新增 `statistical_analysis_draft` 和
  `sample_size_parameter_requirement`，保存完整结果 JSON、输入哈希、生成器版本、参数顺序、
  状态、单位与确认依据。
- 前端新增 STEP14 统计计划面板，展示设计、主要终点、终点类型状态、生成器、分析候选、
  效应量、置信区间、样本量参数表、专家待确认项和限制；STEP13 折叠面板同步展示统计章节 v2。
- 新增统计草案单元测试，并扩展内存、HTTP、真实 PostgreSQL/MinIO、空库迁移和 Playwright
  流程。后端默认全量 68 个测试：57 个通过、11 个环境测试条件跳过、0 失败。
- 空库由应用角色从 V1 连续迁移到 V14，共 34 张 public 表；真实 API 验证 1 份统计草案、
  8 项空值参数、18 个章节和 19 个章节版本，随后全部清理。
- 前端 ESLint、TypeScript、Vitest 1 项、Playwright Chromium 2 项、生产构建和生产依赖审计
  全部通过；生产依赖 0 漏洞，保留既有主包超过 500 kB 告警。

### 阶段 2 第十三切片：STEP15 研究主张—引用依据验证

- STEP14 完成后原子排队 `STEP_15_VALIDATE_CLAIMS_AND_CITATIONS`；STEP15 成功后发布
  `CLAIMS_AND_CITATIONS_VALIDATED` 和最终 `TASK_COMPLETED`。
- 新增 `ClaimCitationValidationService` 与
  `deterministic-claim-citation-linker/v1`，以 STEP13/14 当前方案、STEP08 PubMed 结果和
  STEP10 核验结果为输入，使用输入 SHA-256 幂等回放。
- 从 `BACKGROUND`、`RESEARCH_STATUS` 和 `RESEARCH_GAP` 拆分事实性主张；PMID 只能来自
  章节来源标识，并且必须同时存在于已验证 PubMed 记录和 STEP10
  `VERIFIED` / `VERIFIED_WITH_METADATA_DIFFERENCES` 结果。
- 每条引用链接保存来源类型、PMID、DOI、已核验题名、支持等级、证据范围、摘要依据片段、
  片段位置、片段 SHA-256、元数据核验状态和人工确认状态。
- 当前没有合法开放 PMC 全文，因此有摘要的候选关系标记 `ABSTRACT_ONLY`，无可追溯引用时
  标记 `NEEDS_EXPERT_REVIEW` 并明确记录证据不足；不输出 `SUPPORTED` 或 `FULL_TEXT`。
- Flyway V15 新增 `claim_citation_validation_task`、`research_claim` 和
  `claim_citation_link`，全部包含 `hospital_id`，并通过外键、唯一约束和状态约束保持证据链一致。
- 前端新增 STEP15 主张—依据面板，展示主张状态、专家确认状态、PMID/DOI、核验题名、摘要片段、
  片段哈希和证据不足提示。
- 新增确定性链接服务测试，并扩展内存、HTTP、真实 PostgreSQL/MinIO、空库迁移和 Playwright
  流程。后端默认全量 69 个测试：58 个通过、11 个环境测试条件跳过、0 失败。
- 空库由应用角色从 V1 连续迁移到 V15，共 37 张 public 表；真实 API 验证主张、摘要引用链接、
  片段哈希、专家待确认状态和清理闭环，主库同步迁移到 V15。
- 前端 ESLint、TypeScript、Vitest 1 项、Playwright Chromium 2 项、生产构建和生产依赖审计
  全部通过；生产依赖 0 漏洞，保留既有主包超过 500 kB 告警。
- STEP15 不需要模型自由判断即可完成严格证据边界，因此本轮未调用 DeepSeek，也未读取令牌。

### 阶段 2 第十四切片：STEP16 STROBE 报告完整性预检查

- STEP15 完成后原子排队 `STEP_16_CHECK_STROBE_COMPLETENESS`；STEP16 成功后发布
  `STROBE_COMPLETENESS_CHECKED` 和最终 `TASK_COMPLETED`。
- 新增 `StrobeCompletenessService`、`StrobeChecklistRegistry` 与
  `deterministic-strobe-2007-precheck/v1`，按 STROBE 合并清单的 22 个主条目执行
  版本化、可重放的报告完整性预检查，并记录官方来源引用。
- 每项仅输出 `COVERED`、`PARTIALLY_COVERED`、`MISSING`、`NOT_APPLICABLE` 或
  `NEEDS_EXPERT_REVIEW`，同时保存章节映射、匹配片段、说明、补充建议和专家审核要求。
  结果不包含总分、百分比、等级或排名。
- 方案阶段尚未形成结果和讨论文本，相关条目保持 `NEEDS_EXPERT_REVIEW`；8 项样本量参数
  均缺失时，样本量条目明确标记 `MISSING`，不猜测参数或结论。
- Flyway V16 新增 `strobe_completeness_check_task` 和
  `strobe_completeness_check_item`，全部包含 `hospital_id`，并通过 22 项计数校验、状态约束、
  唯一约束和外键保持结果一致。
- 前端新增 STEP16 STROBE 面板，展示五类计数、22 项折叠明细、映射章节、依据片段、
  补充建议、官方清单链接和“不替代专家审核、不用于研究质量评分”的边界声明。
- 新增确定性预检查单元测试，并扩展内存、HTTP、真实 PostgreSQL/MinIO、空库迁移和
  Playwright 流程。后端默认全量 70 个测试：59 个通过、11 个环境测试条件跳过、0 失败。
- 空库由应用角色从 V1 连续迁移到 V16，共 39 张 public 表；真实 API 验证 1 份预检查任务、
  22 项明细、五类计数、不含质量评分字段及清理闭环，主库同步迁移到 V16。
- 前端 ESLint、TypeScript、Vitest 1 项、Playwright Chromium 2 项、生产构建和生产依赖审计
  全部通过；生产依赖 0 漏洞，保留既有主包超过 500 kB 告警。
- STEP16 使用确定性规则即可完成报告完整性预检查，因此本轮未调用 DeepSeek，也未读取令牌。

### 阶段 2 第十五切片：STEP17 专家审核闭环

- STEP16 完成后不再发布最终 `TASK_COMPLETED`，而是创建版本化审核任务并将 Agent 任务切换到
  `STEP_17_WAIT_EXPERT_REVIEW` / `WAITING_CONFIRMATION`，发布 `EXPERT_REVIEW_REQUIRED`。
- 新增 `ExpertReviewService`、`ExpertReviewController`、内存与 PostgreSQL 双实现；批注只能
  锚定当前方案的具体章节版本或当前 STROBE 条目，不能提交游离于审核快照之外的意见。
- 后端同时校验医院、课题成员关系和 `EXPERT` 角色。普通医生、仅查看成员、同院未加入课题者
  和跨院用户均不能替代专家完成审核。
- 专家可选择 `APPROVE` 或 `RETURN_FOR_REVISION`；退回修改前强制至少存在一条可定位批注，
  避免无法执行的空退回。所有决定使用乐观版本阻止并发覆盖。
- 专家通过后任务仍保持人工等待；只有课题 `OWNER` 可以最终确认。确认事务将方案状态更新为
  `APPROVED`、18 个当前章节更新为 `LOCKED`、STEP17 运行记录写入确认人/时间，并进入
  `STEP_18_EXPORT_DOCUMENT`。
- Flyway V17 新增 `research_review_task`、`research_review_comment` 和
  `research_review_action`，全部包含 `hospital_id`，并通过状态机约束、目标约束、唯一约束、
  外键和不可变操作历史保持审核链一致。
- 前端新增 STEP17 专家审核工作台，展示审核状态、版本、章节锁定状态、不可变批注和时间线；
  EXPERT 可按医学、统计学、报告规范或综合类型批注并作出决定，课题负责人可完成二次确认。
- 新增退回分支单元测试并扩展 HTTP 权限、真实 PostgreSQL/MinIO、空库迁移和 Playwright
  流程。后端默认全量 71 个测试：60 个通过、11 个环境测试条件跳过、0 失败。
- 空库由应用角色从 V1 连续迁移到 V17，共 42 张 public 表；真实 API 验证专家批注、审核通过、
  负责人确认、4 条审核历史、方案批准、18 个章节锁定及清理闭环，主库同步迁移到 V17。
- 前端 ESLint、TypeScript、Vitest 1 项、Playwright Chromium 2 项、生产构建和生产依赖审计
  全部通过；生产依赖 0 漏洞，保留既有主包超过 500 kB 告警。
- STEP17 是人工审核和确定性状态管理，不需要模型自由判断，因此未调用 DeepSeek，也未读取令牌。

### 阶段 2 第十六切片：STEP18 受控 Word 导出与主链闭环

- STEP17 负责人确认后进入 `STEP_18_EXPORT_DOCUMENT` / `WAITING_CONFIRMATION`；只有课题
  `OWNER` 可以选择本院已发布模板并确认导出，成功后 STEP18 运行记录和 Agent 任务均标记完成。
- 新增模板管理与导出服务、HTTP API、内存/PostgreSQL 双实现和 MinIO 对象存储闭环。医院管理员
  可安装内置匿名模板、上传版本并发布，普通成员不能管理模板。
- DOCX 模板协议仅开放 15 个白名单占位符，并拒绝宏、ActiveX、嵌入对象、外部关系、未知或
  未解析占位符；正文、表格、页眉和页脚均支持确定性替换。
- 导出前重新校验方案为 `APPROVED`、18 个当前章节已锁定、专家审核快照一致，以及 REFERENCES
  中每个 PMID 同时存在于 PubMed 记录和 STEP10 已核验结果；引用采用机构数字样式并明确
  摘要级证据边界，不允许模板或模型自由生成 PMID/DOI。
- Flyway V18 新增 `document_template_version` 和 `document_export_record`，保存医院、课题、
  任务、方案、审核、模板版本、确认人/时间、引用样式、方案/引文快照哈希、文件 SHA-256、
  大小和对象键；每个 Agent 任务只允许产生一个最终导出。
- 前端新增 STEP18 模板管理、发布模板选择、明确确认、导出元数据和受权限保护下载入口；
  Playwright 覆盖从模板展示到确认导出和下载链接呈现。
- 后端默认全量 74 个测试：63 个通过、11 个环境测试条件跳过、0 失败。空库由应用角色从
  V1 连续迁移到 V18，共 44 张 public 表；真实 PostgreSQL/MinIO API 从 STEP01 连续执行到
  STEP18，验证模板发布、快照与引文校验、DOCX 存储/下载/解析、文件哈希、任务完成和清理闭环。
- LibreOffice 26.2.4.2 对服务实际导出的 DOCX 完成逐页渲染验收：页眉、表格、正文和引用区
  无截断、错位或残留占位符。前端 ESLint、TypeScript/Vite 构建、Vitest、Playwright 和
  生产依赖审计通过，生产依赖 0 漏洞，保留既有主包超过 500 kB 告警。
- STEP18 是受控模板填充和确定性证据核验，不需要模型自由判断，因此未调用 DeepSeek，也未读取令牌。

### 阶段 6 第二切片：医院级引用格式与模板试生成

- 新增医院隔离、版本化的引用格式管理。医院管理员可以创建、查看和发布引用格式版本，普通医生
  无权管理；历史发布版本归档但仍可被既有导出记录追溯。
- 当前受控支持 `VANCOUVER` 和 `GB_T_7714` 两种数字布局，可配置作者显示上限、作者省略标记、
  DOI 显示和摘要证据范围标记。PMID 强制保留，避免医院配置破坏引用真实性与可追溯性。
- STEP18 导出确认新增已发布引用格式版本选择；引用快照哈希覆盖格式版本 ID、布局和全部参数，
  `document_export_record` 保存格式版本外键、格式代码和不可变版本标签。
- Flyway V19 新增 `citation_style_version` 和相关医院级唯一索引、发布状态约束，并为
  `document_export_record` 增加引用格式版本外键。空库 V1～V19 迁移验证通过，共 45 张
  public 表，主库同步迁移至 V19。
- 模板管理新增匿名试生成：医院管理员可在发布前下载用全套安全示例字段填充的 DOCX，提前检查
  模板字体、表格、页眉页脚、分页和占位符替换，不会带入真实课题内容。
- 真实 PostgreSQL/MinIO API 再次从 STEP01 连续执行至 STEP18，验证 GB/T 7714 格式创建/发布、
  模板试生成、格式版本关联、引用快照、DOCX 下载解析和清理闭环。
- 后端默认全量 76 个测试：65 个通过、11 个环境测试条件跳过、0 失败；前端 ESLint、
  TypeScript、Vitest、Playwright 2 条流程、生产构建和生产依赖审计通过，生产依赖 0 漏洞。
- 使用 LibreOffice 26.2.4.2 对 GB/T 7714 配置生成的实际 DOCX 完成逐页渲染检查，`[J]`、
  PMID、DOI 和摘要证据标记显示正常，无截断、错位或未解析占位符。
- 本切片为确定性格式配置和模板填充，未调用 DeepSeek，也未读取令牌。阶段 6 仍等待至少
  2 套真实脱敏医院模板，不能提前标记达到阶段退出条件。

### 阶段 6 第三切片：两套合成匿名 DOCX 模板回归基线

- 在真实脱敏医院模板尚未取得的情况下，生成两套明确标记为合成测试材料的受版本控制 DOCX：
  模板 A 采用叙事型观察性研究方案布局，模板 B 采用紧凑表格式送审布局。两者均不使用医院
  名称、Logo、患者信息或真实课题内容，不代表任何真实医院格式。
- 初始两套模板完整覆盖 STEP18 的 15 个文本白名单占位符；v2 切片随后将模板 B 的研究目标和
  参考文献替换为结构化列表/重复行指令。两套模板均在正文、附页、页眉页脚和文档元数据中
  标记 `SYNTHETIC_ANONYMOUS / TEST_ONLY`。新增生成脚本
  `tools/generate-synthetic-docx-templates.py`，模板作为后端测试资源纳入版本控制。
- 扩展 `ControlledDocxTemplateEngineTest`：逐套校验占位符集合与白名单完全一致，通过实际
  `ControlledDocxTemplateEngine` 填充安全示例字段，并检查正文、表格、页眉页脚、PMID、
  合成材料标识、残留占位符和可选 QA 输出。
- 使用 LibreOffice 26.2.4.2 对两套原模板和两套实际填充结果共 8 页逐页原始分辨率检查。
  修正 LibreOffice/Apache POI 往返后的首段页眉遮挡、表格跨页和附页顶部定位后，最终版无
  截断、重叠、错位或残留占位符。
- 后端默认全量 77 个测试：66 个通过、11 个环境测试条件跳过、0 失败。本切片未调用
  DeepSeek，也未读取令牌。
- 两套文件仅建立模板协议和渲染回归基线，不计入阶段 6 的真实模板退出条件。后续取得至少
  2 套真实脱敏医院模板后，仍需重新适配 Logo、简单列表、简单重复区域并完成医院方验收。

### 阶段 6 第四切片：受控 DOCX v2 结构化指令

- 将占位协议升级为 `controlled-docx-placeholders/v2`，保持原 15 个文本占位符兼容，并新增
  `${project.logo}`、5 类简单列表指令及 `${repeat.ref.index}` /
  `${repeat.ref.text}` 参考文献表格重复行。
- 发布前校验结构化指令必须独占段落；引用序号和正文指令必须同时位于同一表格行。未知指令、
  混排指令和表格外重复指令均拒绝发布。
- 列表渲染创建真实 Word numbering 定义并保持模板段落样式；引用渲染按已核验引用复制模板行，
  去除输入中的既有序号前缀并重新生成稳定序号。
- Logo 渲染仅允许 1MB 内 PNG/JPEG，拒绝伪造扩展名和无法解码的图片，限制宽高不超过
  2000 像素，并在 480×160 像素边界内等比缩放；未绑定 Logo 时安全清空占位段落。
- 合成模板 B 已使用 Logo、Word 项目符号列表和参考文献重复行，并通过实际上传、校验、预览、
  发布、专家锁定快照、机构引用格式和 STEP18 导出闭环。模板 A 继续验证 v1 文本占位兼容。
- LibreOffice 26.2.4.2 对两套原模板、两套填充模板和 STEP18 实际导出逐页检查；最终均为
  2 页，无截断、重叠、错位或残留占位符。
- 后端全量 80 个测试：69 个通过、11 个环境测试条件跳过、0 失败、0 错误。本切片为确定性
  DOCX 处理，未调用 DeepSeek，也未读取令牌。
- v2 能力已完成，但真实医院 Logo 资产与版式仍必须在至少 2 套脱敏医院模板到位后绑定和验收，
  因此阶段 6 退出条件仍未满足。

## 2026-07-29 阶段 7 第一切片：安全与部署基线

- 执行仓库级 Codex Security 标准扫描，当前快照 413/413 个文件完成覆盖；两条既有高价值候选
  （原型接口匿名/CSRF、外部模型缺少最终出站门禁）在当前代码中均由回归证据关闭。
- `/api/prototype/**` 不再匿名放行或绕过 CSRF；浏览器客户端统一携带凭据并使用
  `XSRF-TOKEN` / `X-XSRF-TOKEN`。
- 新增 `ExternalModelInputGuard` 与 Prompt Injection 规则，DeepSeek 在 Prompt 渲染前统一
  执行长度、敏感标识和指令注入检查；拒绝路径经 WireMock 验证为 0 次外部请求。
- 敏感内容规则扩展到邮箱、姓名、出生日期、地址、医保/健康卡号；错误信息只返回规则码，
  不回显被阻断内容。
- PubMed XML 增加 `FEATURE_SECURE_PROCESSING`、外部 DTD/Schema 禁止及空实体解析器；
  保留 NCBI EFetch 合法 DOCTYPE 的协议兼容，相关 Java 21 测试通过。
- 增加跨医院 HTTP 资源族回归，覆盖项目、任务、列表、取消和成员标识；跨院访问保持阻断。
- 前端升级 ESLint/Vue 测试工具链并固定 `js-beautify` 安全版本；完整 `npm audit`
  对 341 个依赖报告 0 漏洞，CI 新增 `npm audit --audit-level=high`。
- 安全扫描成功封存 2 个低风险生产配置项：会话 Cookie 的本地默认值可被误带入生产、
  Swagger/OpenAPI 在共享 profile 中匿名开放。随后新增 `prod` 安全 profile 并修复：
  生产环境强制 Cookie `Secure`、关闭文档端点，安全链在 `prod` 下也不匿名放行文档路由。
- 新增 `ProductionSecurityConfigurationTest`，即使测试显式注入不安全覆盖值，仍验证生产
  Cookie 含 `Secure` 且 Swagger/OpenAPI 返回 403；原 `memory` 开发行为测试继续通过。
- 后端全量 87 个测试：76 个通过、11 个 Docker/外部服务条件测试跳过、0 失败。前端 lint、
  typecheck、Vitest、生产构建和完整依赖审计通过。
- OWASP Dependency-Check 12.2.2 首次 NVD 同步仍在后台运行，尚未生成报告，因此不宣称
  Java 依赖 SCA 通过；Docker/镜像扫描继续作为环境项。

### ClamAV 实机接入

- 用户完成 ClamAV 1.5.3 与 2026-07-29 病毒库安装后，新增本机 `clamd.conf`，守护进程
  只监听 `127.0.0.1:3310`，禁用远程关闭命令，并按应用 20MB 上传上限设置 25MB 流限制。
- `clamd` 实际加载 3,627,981 条签名；新增可重复启动脚本，后端本地启动会检查并按需启动
  ClamAV，生产 profile 强制使用 `clamav` 文件扫描模式。
- 项目扫描器实机测试验证安全文本返回 `CLAMAV/clean`、标准 EICAR 内容返回 `FOUND`；
  另以未监听端口验证连接失败时抛错并阻止文件入库。
- 将 PostgreSQL/MinIO API 联调测试改为按实际扫描模式断言；以 `FILE_SCAN_MODE=clamav`
  真实完成认证 DOCX 上传、ClamAV 放行、文本提取、MinIO 隔离对象写入和 PostgreSQL
  `scan_engine=CLAMAV` 持久化，随后清理对象与测试数据。
- ClamAV 显式实机测试 3/3 通过；后端默认全量 89 个测试中 77 个通过、12 个环境测试
  条件跳过、0 失败。重新打包并以 PostgreSQL、MinIO、ClamAV 模式启动后端成功。

### README 与详细架构文档

- 重写根目录 README，补齐项目边界、当前状态、技术栈、仓库结构、三种运行模式、快速启动、
  关键配置、全部接口分组、18 步工作流、数据库/对象路径、测试、CI、运维和外部验收项。
- 新增 `doc/架构设计/系统详细架构设计.md`，以当前代码为事实源记录系统上下文、容器和组件、
  Profile/适配器矩阵、认证与多医院隔离、任务租约/SSE、外部工具、文件安全、DOCX、数据关系、
  事务/幂等/补偿、安全控制、部署拓扑、容量边界及演进不变量。
- 同步更新本地中间件和 ADR-0009 的 ClamAV/V19 状态，删除 `.env.example` 中重复且冲突的
  PubMed 配置键。
- 文档核对结果：README 约 680 行、详细架构约 1050 行；本地链接、Markdown 代码围栏、尾随空白、
  明文 Token 模式、环境变量重复键均通过检查；Flyway 19 个文件和 44 张业务表计数与正文一致。
