# 本地中间件状态

> 检查时间：2026-07-29（Asia/Shanghai）

## PostgreSQL

- 安装目录：`D:\develop\postgresql18`
- 数据目录：`D:\develop\postgresql18\data`
- 版本：18.4
- 地址：`127.0.0.1:5432`
- 数据库/角色：`medical_agent`
- pgvector：0.8.0
- Flyway：V1～V19 已成功执行，共 44 张业务表和 `flyway_schema_history`，合计 45 张 public 表
- 网络：只监听本机回环地址

pgvector 由数据库管理员预装；应用角色保持非超级用户。

## MinIO

- 可执行文件：`D:\develop\minio\minio.exe`
- 数据目录：`D:\develop\minio\data`
- API：`http://127.0.0.1:9000`
- 控制台：`http://127.0.0.1:9001`
- 状态：运行中，健康检查 HTTP 200
- 存储桶：`medical-agent-files`（由应用幂等创建）
- 联调：文件、PubMed、ClinicalTrials.gov、Crossref 原始响应、受控模板和导出 DOCX 的写入、
  哈希校验、读取、删除闭环通过，测试对象已清理
- PID 文件：`D:\develop\minio\minio.pid`
- 凭据：Windows DPAPI 加密文件 `D:\develop\minio\.medical-agent-credential.xml`
- 官方客户端：`D:\develop\minio\mc.exe`，RELEASE.2025-08-13T08-35-41Z；
  官方 SHA-256 校验通过

凭据文件只允许当前 Windows 用户解密，不应复制到仓库、日志或部署文档。

## ClamAV

- 安装目录：`D:\develop\clamav`
- 版本：1.5.3
- 地址：`127.0.0.1:3310`
- 病毒库目录：`D:\develop\clamav\database`
- 当前签名数：3,627,981
- 配置：`D:\develop\clamav\clamd.conf`
- 网络：仅监听本机回环地址，禁用协议远程关闭
- 联调：安全内容、EICAR、不可用时失败关闭，以及真实 DOCX→MinIO→PostgreSQL
  `scan_engine=CLAMAV` 闭环均通过

## 应用运行健康

- 前端：`http://127.0.0.1:5173`，当前 HTTP 200
- 后端：`http://127.0.0.1:8080`，当前运行中；实时 PID 由状态脚本显示
- 总健康：`GET /actuator/health`，当前 HTTP 200 / `UP`
- 进程探针：`GET /actuator/health/liveness` 与 `/readiness`，当前均 HTTP 200
- 响应边界：匿名可读但只返回整体状态，不暴露组件和详情
- 实机依赖：PostgreSQL、MinIO、ClamAV 健康均已纳入总健康或本地状态检查

仓库根目录可执行：

```powershell
.\tools\status-local.ps1
.\tools\smoke-local.ps1
.\tools\stop-local-backend.ps1
.\tools\start-local-backend.ps1
```

状态脚本已验证五项服务均为 `UP`，冒烟脚本 8/8 通过。停服脚本只停止经命令行校验的本项目
后端 Java 进程，不停止中间件或前端。

## 本地恢复点

- 最新有效恢复点：`20260729T150442Z-recovery-point-8b5ee5ed`
- 位置：`artifacts\backups\20260729T150442Z-recovery-point-8b5ee5ed`
- 内容：PostgreSQL custom archive、MinIO mirror、元数据和 SHA-256 manifest
- 校验：`Valid`，45 张 public 表、当前应用桶 0 个对象、恢复点 189,192 字节
- 演练：45 张表逐表行数一致；1 个合成匿名对象恢复后 SHA-256 一致
- 清理：临时恢复数据库为 0，MinIO 只保留正式 `medical-agent-files` 桶

恢复点目录已加入 Git 忽略。它没有生产级加密、异地副本或不可变保留能力，不允许放入真实
患者数据后复制到普通共享位置。

## 当前限制

- 本机没有 Docker，Testcontainers 仍会明确跳过。
- Flyway 11.7.2 对 PostgreSQL 18.4 给出版本支持提示，需要持续关注依赖升级。
- 课题、文件、身份、医院、会话、审计、课题成员、Agent 任务/步骤/事件和
  STEP08～STEP18 的文献、设计、方案、审核和导出记录均已接入 JDBC。
