# 阶段 0 验证报告

> 验证日期：2026-07-26  
> 结论：不依赖外部输入的纵向原型已完成；阶段 0 未达到退出条件。

## 已验证能力

| 能力 | 结果 | 证据 |
|---|---|---|
| Java 21 编译 | 通过 | Temurin 21.0.11 + Maven 3.9.11 |
| 研究想法结构化 | 通过 | `research-analysis/v1` |
| 缺失信息识别 | 通过 | 匿名示例返回 4 个澄清问题 |
| 三个观察性方向 | 通过 | 横断面、队列、病例对照 |
| 方向确认与 PECO | 通过 | `DIR-02` + `peco/v1` |
| 文献引用来源约束 | 通过 | 2 条 `PUBMED_MOCK_SNAPSHOT`，含 PMID/DOI |
| DOCX 受控模板填充 | 通过 | 无未知或残留占位符 |
| DOCX 视觉渲染 | 通过 | LibreOffice 26.2.4.2，最终产物共 1 页，逐页检查无版式缺陷 |
| 敏感数据规则 | 通过 | 身份证与病案号命中后阻断外部模型 |
| HTTP 运行验证 | 通过 | 实际 JAR 启动并调用分析、确认、导出接口 |
| 前端原型 | 通过 | ESLint、类型检查、Vitest、Vite 构建 |

## 未通过或未执行

| 项目 | 状态 | 原因 |
|---|---|---|
| Testcontainers / Flyway 空库迁移 | 跳过 | 本机没有 Docker |
| PostgreSQL 与 MinIO 连接 | 未执行 | 本机没有 Docker |
| Docker Compose 配置检查 | 未执行 | Docker 命令不可用 |
| 真实模型结构化输出 | 未执行 | 供应商、模型和数据政策未确认 |
| 真实 PubMed/Crossref/ClinicalTrials.gov | 未执行 | 当前纵向原型按要求使用 Mock |
| 真实医院 DOCX 模板 | 未执行 | 未提供脱敏模板 |
| 专家验收 | 未执行 | 未提供匿名案例和专家 |

## 阶段 0 退出条件判定

- 至少一个模型稳定输出结构化 JSON：**未满足**（只有确定性 Mock）。
- 至少一个真实模板成功填充：**未满足**（只有匿名示例模板）。
- PubMed 检索、详情和 PMID 验证可运行：**部分满足**（Mock 快照通过，真实 API 未验证）。
- 专家确认流程合理：**未满足**。
- 患者数据边界和外部模型策略书面确认：**工程 ADR 已完成，仍需项目方/医院确认**。
