spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/medical_agent}
    username: ${DATABASE_USERNAME:medical_agent}
    password: ${DATABASE_PASSWORD:}
  flyway:
    enabled: true
medical:
  security:
    secure-cookie: true
